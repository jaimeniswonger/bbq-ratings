info:
  component: API Gateway
  
endpoints:
  restart:
    enabled: true
  shutdown:
    enabled: true
  health:
    sensitive: false

zuul:
  routes:
      restaurants:
        path: /api/**
        serviceId: restaurants

server:
  port: 8765

logging:
  level:
    ROOT: INFO
    org.springframework.web: DEBUG
